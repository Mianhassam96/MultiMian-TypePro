# MultiMian — Full-stack development website (Next.js)

This is a starter Next.js + TypeScript + Tailwind project for the MultiMian website.

Quick start

```bash
# from project root
npm install
npm run dev
```

Notes
- Contact form posts to `/api/contact` (placeholder that logs the payload). Hook up an email/CRM provider in `pages/api/contact.ts`.
- Tailwind is configured; run `npm install` to get required packages.
